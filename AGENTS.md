# Vermouth

One repository, four Go services plus a gateway, a React web app, four Postgres databases, and
Redpanda between them. Services never call each other: facts travel as events (spec 0001).

## Stack

- **Language / Runtime**: Go 1.27 (six modules under `go.work`), TypeScript on Node 24.19 (`web/`)
- **Framework**: standard library `net/http` with `ServeMux` pattern routing; React 19 with Vite in `web/`
- **Key dependencies**: `pgx` v5 with `sqlc`, `franz-go` (Redpanda), `goose`, `golang-jwt/jwt/v5` (Ed25519)
- **Data**: Postgres 18, one instance per service; Redpanda single node; Garage over the S3 API
- **Package manager**: `go.work` for Go, pnpm 11.22.0 for `web/`, `Taskfile.yml` (go-task) over both halves

Per workspace detail lives in that workspace's own `AGENTS.md`, listed under `## Context files`.

## Build approach

**Tracer Bullet** (prove the whole pipe works end to end before building any part of it fully).

## Commands

```bash
# Install (once): .env, signing keys, web dependencies
task env && task dev:keys && task web:install

# Dev server: infra, migrations, build, then every service
task dev            # then `task thread` drives the one end to end thread
task web:dev        # the Vite dev server, separately

# Build
task build          # every Go binary into bin/, CGO_ENABLED=0
task web:build      # the web app to static files

# Test
task test           # go test ./... in every module

# Container images: FROM scratch, built with the repository root as the build context (STK-24)
task image -- identity   # one service
task images              # all five
```

`task` on its own lists everything. Generated code is regenerated by `task generate` and committed.
`test/compose.test.yaml` is the one infra stack behind `task infra:up`, shared by development and the
tests, and `task infra:clean` deletes its data.

## Specs

Stored in `docs/specs/`. Format: `docs/specs/NNNN-title/index.md` plus its `rationale.md`.
Spec 0001 fixes the service boundaries and the event catalogue (`INV-n`), spec 0002 the stack
(`STK-n`). Both are Accepted, and the rules below only say how they are met.

The feature roadmap lives in `docs/scope/scope.md`: twenty features in phases with their status, the
workflow level (Beta, so `$develop` then `$check verify` then `$test`), and the decisions taken up
front so no feature reopens them. `$scope` owns that file.

## Rules

- Each service is a bounded context. It owns its entities, its database, its migrations, and its
  role, and never reads another service's database (INV-2, STK-5).
- Facts cross a service boundary only as events on the owning service's topic, never as a service
  to service call (INV-1). A copy of another service's fact is a projection, rebuildable by a
  replay; an authoritative record (an invoice, its number, paid state) never is.
- Event names, envelope fields, and topic names are taken verbatim from spec 0001's catalogue, past
  tense, so one name means one value everywhere. The domain language is the tutor's own: tutor,
  student, class, session, attendance, rate, invoice.
- The aggregate is the consistency boundary: one transaction touches one aggregate, and anything
  across a context line is referenced by its id (`tutor_id`, `class_id`, `session_id`, `student_id`,
  `invoice_id`), never by a shared struct. A consumer reads another context's events tolerantly,
  taking only the fields it needs and ignoring the rest (INV-12), which is where this repo puts the
  anti corruption layer.
- The business write and its outbox insert happen in the same `pgx.Tx`, in the same function
  (INV-3, STK-4). Nothing publishes to the broker from a handler.
- Every piece of cross service machinery lives once in `pkg/vermouth` (STK-2), never copied into a
  service. Inside a service, `internal/store` is the only path to the database, `internal/handler`
  holds the work, `internal/http` only routes, `internal/consumer` is the only writer of a
  projection. The gateway holds no business rule. Layout follows the scaffold
  (`cmd/<name>/main.go`, `internal/{http,handler,store,consumer}`, `db/{migrations,queries}`), so new
  code joins an existing layer rather than inventing one.
- Every `cmd/<name>/main.go` is a thin `main` calling `run() error`, and blank imports
  `_ "time/tzdata"`, because a `FROM scratch` image carries no system zone database while a timezone
  name still has to resolve (STK-6).
- Every exported Go symbol carries a doc comment, and every package a package comment, the way
  `pkg/vermouth/doc.go` does. Say why, not what.
- One error shape at the boundary, `vermouth.APIError`. Wrap with `%w` inside. Configuration comes
  from the environment only, parsed once at startup, and a missing required variable stops startup
  with `vermouth.MissingEnvError` rather than defaulting silently (STK-8).
- A boundary answer goes through `vermouth.WriteError` (the `APIError` shape, carrying the
  `request_id`) or `vermouth.WriteJSON`, never a hand rolled struct or `http.Error`.
- Money is `int64` counting dong in Go and `bigint` in the database, formatted only at the edges
  (STK-7). Identifiers are UUIDv7 generated in Go; timestamps are `timestamptz` in UTC.
- SQL is hand written in `db/queries/` and compiled to typed Go by `sqlc` (STK-3). `api/openapi.yaml`
  is the only source of gateway types, Go through `oapi-codegen` and browser through
  `openapi-typescript` (STK-10). Never hand write either.
- In `web/`: TypeScript is strict (no `any`, exhaustive types), exports are named only, and every
  screen meets WCAG AA with visible focus, keyboard reach, and phone first sizing. `"strict": true`
  is missing from `web/tsconfig.app.json` today, so turning it on is the first job of
  `$develop tooling`.
- Commit messages are conventional: `feat:`, `fix:`, `docs:`, `chore:`, `test:`, `refactor:`.

## Tooling

Chosen here, installed by `$develop tooling`. None of it is wired up yet.

- **Go**: `golangci-lint` on the pinned upstream strict config (see the `golangci-lint-strict`
  skill), plus `gofumpt` for formatting. The `golangci-lint` binary is already installed at 2.13.1,
  so what is missing is the versioned config file and the task target; `gofumpt` is not installed.
  `task vet` and `task fmt` are the current stand ins.
- **Web**: keep `oxlint` (already a dev dependency), add Prettier for TypeScript and CSS.
- **Type check**: `tsc -b` (already inside `pnpm build`) for the web app.
- **Before each commit**: one `task check` target running format, lint, and type check, wired to a
  `pre-commit` hook. No hook is installed today.
- **Tests**: Go `testing` with `testify/require`; anything touching a relay, a consumer, a
  projection, or a month end run runs against the real Postgres and Redpanda in
  `test/compose.test.yaml` (STK-15). Playwright for the money path in the browser. `$test` sets the
  runners up; only `pkg/vermouth/envelope_test.go` exists so far.
- **CI**: one GitHub Actions workflow on push and on pull request: build, lint, type check, test.
  There is no `.github/` directory yet.

## Git

- integration: on
- branch prefix: `feat/`
- commit: per-milestone
- Pushing and opening a pull request always ask you first.

## Agent skills

Installed in `.agents/skills/` (tool agnostic), symlinked into `.claude/skills/`. Repo wide skills
are listed here; the web app's own are in `web/AGENTS.md`.

Project workflow skills use Codex's `$skill-name` syntax. Their canonical copies live in
`.agents/skills/`; `.claude/skills/` contains compatibility symlinks for Claude Code.

- `$architect`: make load bearing design decisions and write specs under `docs/specs/`
- `$scope`: plan and maintain the feature roadmap under `docs/scope/`
- `$develop`: implement a scoped feature or tooling milestone
- `$check`: review or verify a change before merge
- `$test`: add focused tests for the current change
- `$debug`: reproduce, diagnose, and fix a defect
- `$audit`: bootstrap or gap fill durable `AGENTS.md` context
- `$sync`: reconcile context, scope tasks, and spec statuses with repository evidence
- `$document`: produce release notes, changelogs, pull request text, or postmortems

- [golang-code-style](.agents/skills/golang-code-style/): `samber/cc-skills-golang`, idiomatic Go naming, structure, and readability
- [golang-error-handling](.agents/skills/golang-error-handling/): `samber/cc-skills-golang`, wrapping, sentinel errors, and error types
- [golang-testing](.agents/skills/golang-testing/): `samber/cc-skills-golang`, table tests, subtests, and fixtures
- [golang-stretchr-testify](.agents/skills/golang-stretchr-testify/): `samber/cc-skills-golang`, `testify/require` conventions
- [golang-concurrency](.agents/skills/golang-concurrency/): `samber/cc-skills-golang`, goroutines, channels, and `errgroup`, which every `main.go` uses
- [golang-context](.agents/skills/golang-context/): `samber/cc-skills-golang`, cancellation and deadlines through the request and consumer paths
- [golang-database](.agents/skills/golang-database/): `samber/cc-skills-golang`, `pgx` pools, transactions, and query patterns
- [golang-project-layout](.agents/skills/golang-project-layout/): `samber/cc-skills-golang`, the `cmd` and `internal` layout this repo already follows
- [golang-security](.agents/skills/golang-security/): `samber/cc-skills-golang`, input handling, secrets, and crypto use
- [golang-observability](.agents/skills/golang-observability/): `samber/cc-skills-golang`, `log/slog` structured logging (STK-9)
- [golang-lint](.agents/skills/golang-lint/): `samber/cc-skills-golang`, what the linters ask for and why
- [golangci-lint-strict](.agents/skills/golangci-lint-strict/): `metalagman/agent-skills`, the pinned strict config chosen above
- [go-goose](.agents/skills/go-goose/): `metalagman/agent-skills`, writing and running the per service migrations (STK-21)
- [kafka-development](.agents/skills/kafka-development/): `mindrally/skills`, producer, consumer group, and partitioning practice behind `pkg/vermouth`
- [openapi](.agents/skills/openapi/): `oakoss/agent-skills`, keeping `api/openapi.yaml` honest as the one gateway contract
- [pnpm](.agents/skills/pnpm/): `antfu/skills`, the workspace and package manager conventions for `pnpm-workspace.yaml`

Declined: sqlc, oapi-codegen, pgx by name, Task (Taskfile), franz-go specifically (no skill exists; the Go database and Kafka skills above are the closest fit).
MCP servers: none connected. Avoid `@modelcontextprotocol/server-postgres` (archived, its read only mode was bypassable); Redpanda's own MCP servers target Redpanda Cloud, not this local broker.

## Context files

- [pkg/vermouth/AGENTS.md](pkg/vermouth/AGENTS.md) (the shared module: envelope, outbox, relay, idempotency, retry and dead letter, config, tokens)
- [gateway/AGENTS.md](gateway/AGENTS.md) (the only public surface: routing, token check, read fan out)
- [services/identity/AGENTS.md](services/identity/AGENTS.md) (tutors, passwords, token minting, the JWKS document)
- [services/teaching/AGENTS.md](services/teaching/AGENTS.md) (classes, sessions, attendance, rates; the publisher of most facts)
- [services/billing/AGENTS.md](services/billing/AGENTS.md) (month end runs, invoices, PDFs; projections versus authoritative records)
- [services/notifications/AGENTS.md](services/notifications/AGENTS.md) (the digest scheduler and the single replica rule)
- [web/AGENTS.md](web/AGENTS.md) (React 19, Vite, TanStack, Tailwind v4, and the generated API types)
- [test/AGENTS.md](test/AGENTS.md) (the one infra stack for development and tests, the service scripts, the thread driver)

_Drafted by $audit from the repo, worth a quick human pass. Edit freely: once a line stops matching this draft, later runs treat it as curated and will flag rather than overwrite it._
