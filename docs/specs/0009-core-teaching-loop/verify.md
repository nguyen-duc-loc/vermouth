# Verify: Core teaching loop · spec 0009 · updated 2026-08-30

_Steps derived from spec 0009 acceptance criteria. `$check verify` runs these; `$test` locks the durable ones._

## UI and manual

- [x] V1. Sign in as tutor A, open home, and compare its tutor with identity `/me` → the identity fields and profile timezone match → AC-7
- [x] V2. Use a token timezone different from the browser timezone and a newly changed identity profile timezone → `request_time_zone` follows the verified token → AC-7, AC-15
- [x] V3. Read home across a timezone date boundary → `local_date` follows the verified token timezone → AC-7
- [x] V4. Inspect `next_local_midnight_at` → it is the first instant of the next calendar date in the request timezone → AC-7, AC-15
- [x] V5. Open setup at several local times → defaults use the next half hour plus one hour, moving to tomorrow when the span leaves today → AC-1, AC-7, AC-11
- [x] V6. Edit every setup value and complete the first step → stored class and session values match the edited inputs → AC-1, AC-2
- [x] V7. Use tutor A identifiers with tutor B → class, session, student, roster, and attendance operations return the same `404` as missing identifiers → AC-6, AC-12
- [x] V8. Inspect newly created class, session, and student identifiers → every identifier is UUIDv7 generated by teaching → AC-2, AC-4
- [x] V9. Submit names with outer whitespace and rates at 0 and 1,000,000,000 → names are trimmed and valid boundary rates persist → AC-2, AC-4, AC-11
- [x] V10. Omit color, compare the stored suggestion with the TypeScript FNV 1a result, then try all seven overrides → stored color is valid and stable after reload → AC-3
- [x] V11. Inspect the class response, teaching row, event, and billing rate row → currency is always `VND` → AC-2, AC-8
- [x] V12. Create a class for a chosen date → `rate_effective_from` and session `local_date` equal that submitted date → AC-2
- [x] V13. Submit valid, missing, and repeated local clock times → valid times store as UTC while missing and repeated times return `400` → AC-2, AC-11
- [x] V14. Inspect the teaching outbox and delivered facts → catalogue names, fields, topic, and partition keys match spec 0001 → AC-2, AC-4, AC-5, AC-8, AC-10
- [x] V15. Create a student with a name and phone, then with an empty phone → UUID, name, exact phone, and null conversion match the validated inputs → AC-4, AC-11
- [x] V16. Search the student event, home response, billing rows, and logs → phone appears in none of them → AC-4, AC-6
- [x] V17. Join a roster using the path class, body student, and first session date, then retry it → the exact retry returns `200` without an event and a different open date returns `409` → AC-4, AC-10, AC-12
- [x] V18. Mark attendance through an owned session and inspect the event → teaching derives `class_id` from that session and rejects foreign resources with `404` → AC-5, AC-6
- [x] V19. Mark Present, correct to Absent, then repeat Absent → the last committed state and UTC `marked_at` survive, while the identical repeat emits no event → AC-5, AC-6
- [x] V20. Inspect each emitted envelope → event id, occurred time, tutor, request id, version 1, topic, and key come from the shared event machinery → AC-2, AC-4, AC-5
- [x] V21. Reload during each create step and inspect requests → one browser UUID is created per step and reused until that step completes → AC-10, AC-14
- [x] V22. Replay a class create with the same key, then change name, color, rate, date, time, or verified timezone → equal input recovers the resource and changed input returns `409` → AC-10
- [x] V23. Replay a student create with the same key, then change name or nullable phone → equal input recovers the student and changed input returns `409` → AC-10
- [x] V24. Interrupt class and student responses after commit, then retry → receipt identifiers recover the original owned resources → AC-10
- [x] V25. Compare first create and replay status codes → the creator receives `201` and the equal replay receives `200` → AC-10
- [x] V26. Delay billing and read home → class name and color still come from teaching and remain correct → AC-3, AC-7, AC-9
- [x] V27. Reload before and after attendance → student name, nullable state, and nullable or saved `marked_at` come from teaching → AC-5, AC-6, AC-7
- [x] V28. Archive a class, cancel a session, remove a student, and close coverage in fixtures → inactive facts are excluded from home → AC-7
- [x] V29. Use a browser timezone different from the token timezone → visible session times use `request_time_zone` and an explicit English locale → AC-7, AC-15
- [x] V30. Inspect each session card without color perception → the class marker is always paired with the class name → AC-3, AC-13
- [x] V31. Load more than 50 sessions with equal start times, then use malformed and prior date cursors → paging has no gap or duplicate and invalid cursors return `400` → AC-7, AC-11, AC-15
- [x] V32. Compare billing status with one tutor's projection tables → the five counts are exact and tenant scoped → AC-8, AC-9
- [x] V33. Compare `latest_updated_at` with classes, rates, sessions, students, roster, and attendance → it is their greatest nonnull update and is null before any fact → AC-9
- [x] V34. Deliver the five teaching facts one at a time → billing stays `waiting` until every displayed count is positive, then becomes `active` → AC-9
- [x] V35. Stop billing, continue setup and attendance, then restart it → home shows a safe unavailable panel and replay converges to one correct projection row → AC-8, AC-9, AC-12, AC-15
- [x] V36. Delay identity, teaching, and billing beyond the shared code deadline → required reads return `502`, while billing degrades only its panel → AC-9, AC-12
- [x] V37. Reload at each setup step in one tab, then switch tutors → values, step, and created identifiers recover only for the same tutor → AC-14
- [x] V38. Inspect session storage during setup, completion, and sign out → versioned tutor scoped draft and command keys persist during work and clear at the required boundaries → AC-14
- [x] V39. Observe billing network calls while waiting → polling runs once per second for at most ten seconds, stops when active or unmounted, and manual retry works → AC-15
- [x] V40. Refocus the window and cross `next_local_midnight_at` → the cursor chain resets and home reads the new local date → AC-15
- [x] V41. Complete setup and attendance with keyboard and screen reader at phone width and 200 percent zoom → caller owned English copy, field errors, focus, 44 pixel targets, and live announcements remain usable → AC-13

## Commands

- [x] C1. `task infra:up && task migrate:up` → migration 00004 applies and live teaching schema contains required class color and command receipt constraints → AC-2, AC-3, AC-10
- [x] C2. `task thread:host` → class, safe replays, student, roster, attendance, home reload, Redpanda delivery, and active billing projection complete → AC-1 through AC-12, AC-16
- [x] C3. `task generate:sql && task generate:api && task web:generate` → generated SQL, Go API, and browser types are current with no old thread contract → AC-16
- [x] C4. `task build && task web:build` → every Go binary and the production web application build → AC-13, AC-16
- [x] C5. `task check` → Go formatting and lint plus Biome and strict TypeScript pass → AC-11, AC-12, AC-13, AC-16
- [x] C6. `task test` → every Go module test passes against the shared stack → AC-2 through AC-12, AC-16
- [x] C7. `cd web && pnpm exec vitest run` → the web unit and component suite passes → AC-13, AC-14, AC-15, AC-16

## Acceptance criteria coverage

- AC-1 covered by V5, V6, V15, V17, V37, C2
- AC-2 covered by V6, V8, V9, V11 through V14, V20, C1, C2, C6
- AC-3 covered by V10, V26, V30, C1, C2
- AC-4 covered by V9, V14 through V17, V20, C2, C6
- AC-5 covered by V14, V18, V19, V27, C2, C6
- AC-6 covered by V7, V16, V18, V19, V27, C2, C6
- AC-7 covered by V1 through V5, V26 through V31, C2
- AC-8 covered by V11, V14, V32, V35, C2, C6
- AC-9 covered by V26, V32 through V36, C2
- AC-10 covered by V14, V17, V21 through V25, C1, C2
- AC-11 covered by V5, V9, V13, V15, V31, C2, C5
- AC-12 covered by V7, V17, V35, V36, C2, C5
- AC-13 covered by V30, V41, C4, C5, C7
- AC-14 covered by V21, V37, V38, C7
- AC-15 covered by V2, V4, V29, V31, V35, V39, V40, C7
- AC-16 covered by C2 through C7
